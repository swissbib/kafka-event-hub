AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    # debug: all
Consumer:
  bootstrap.servers: localhost:9092
  group.id: test-lines-consumer
  default.topic.config:
    auto.offset.reset: smallest
  # debug: all
Topic:
  - test-gz-v2