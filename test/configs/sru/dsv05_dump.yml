AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    debug: all
Producer:
  bootstrap.servers: localhost:9092
  debug: all
Topic:
  topic: test
  num_partitions: 1
  replication_factor: 1
SRU:
  database: defaultdb
  schema: marc/json
  max_records: 100
