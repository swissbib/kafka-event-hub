AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    debug: all
Producer:
  bootstrap.servers: localhost:9092
  debug: all
Topic:
  topic: json
  num_partitions: 1
  replication_factor: 1
path: "data/lines/test_data.json"