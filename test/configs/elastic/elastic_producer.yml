AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    debug: all
Producer:
  bootstrap.servers: localhost:9092
  debug: all
Topic:
  topic: test
  num_partitions: 1
  replication_factor: 1
ElasticIndex:
  index: consumer-test
  doc_type: test
  url: http://localhost:9200
  timeout: 300
  mapping: null
Scroll:
  scroll: 5m
  size: 100
  # match all!
  query: null
