AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    debug: all
Consumer:
  bootstrap.servers: localhost:9092
  group.id: dsv05-marc-test
  debug: all
Topic:
  topics:
    - test
Elastic:
  index: consumer-test
  doc_type: test
  url: http://localhost:9200
  timeout: 300
  mapping: null
