AdminClient:
  conf:
    bootstrap.servers: localhost:9092
    # debug: all
Consumer:
  bootstrap.servers: localhost:9092
  group.id: dsv05
  # debug: all
Topic:
  topics:
    - sru_jsonmarc_dsv05
Elastic:
  index: kafka-dsv05
  doc_type: record
  url: localhost:9200
  timeout: 300
  mapping: null
